type: object
description: |
  Возврат поставщику (purchasereturn). Заглушка: в спецификации нет полного описания сущности;
  при развёртывании в ответе API возможны дополнительные поля.
properties:
  meta:
    description: Метаданные возврата поставщику
    $ref: '../common/meta.yaml'
  id:
    type: string
    format: uuid
    description: ID возврата поставщику
    readOnly: true
  accountId:
    type: string
    format: uuid
    description: ID учетной записи
    readOnly: true
