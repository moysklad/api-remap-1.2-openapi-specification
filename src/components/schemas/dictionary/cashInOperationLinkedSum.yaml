type: object
description: |
  Поле суммы по связанной операции. Комбинируется через `allOf` с телом документа в элементах
  `operations[]` приходного ордера.
required:
  - linkedSum
properties:
  linkedSum:
    type: number
    format: double
    description: Сумма, оплаченная по данному документу
