type: object
required:
  - context
  - meta
  - rows
properties:
  context:
    $ref: '../common/context.yaml'
  meta:
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    items:
      $ref: '../../../openapi.yaml#/components/schemas/Store'
