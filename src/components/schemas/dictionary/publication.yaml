type: object
description: Публикация Заказа покупателя
properties:
  meta:
    description: Метаданные Публикации
    $ref: '../common/meta.yaml'
  template:
    $ref: '../../../openapi.yaml#/components/schemas/EmbeddedTemplate'
  href:
    type: string
    format: uri
    description: Ссылка на страницу Публикации
