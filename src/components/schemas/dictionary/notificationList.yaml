type: object
description: Лента уведомлений
x-implements: EntityList
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../common/context.yaml'
  meta:
    description: Метаданные о выдаче
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    description: Массив уведомлений разных типов, определяемых по meta.type
    items:
      $ref: '../../../openapi.yaml#/components/schemas/NotificationAbstract'
