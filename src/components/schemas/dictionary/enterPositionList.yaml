type: object
description: Список позиций Оприходования
x-implements: IEntityList
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../../../openapi.yaml#/components/schemas/Context'
  meta:
    description: Метаданные о выдаче
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    description: Массив позиций Оприходования
    nullable: true
    items:
      $ref: '../../../openapi.yaml#/components/schemas/EnterPosition'
