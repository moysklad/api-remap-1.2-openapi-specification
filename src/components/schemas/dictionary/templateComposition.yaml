type: object
required:
  - template
  - count
properties:
  template:
    $ref: '../../../openapi.yaml#/components/schemas/EmbeddedTemplate'
  count:
    type: integer
    description: Количество копий печатной формы
    minimum: 1
    maximum: 10