type: object
description: Список пользовательских ролей
x-implements: IEntityList
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../common/context.yaml'
  meta:
    description: Метаданные о выдаче
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    description: Массив пользовательских ролей
    items:
      $ref: '../../../openapi.yaml#/components/schemas/CustomRole'
