type: object
description: Список позиций Счета поставщику
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../../../openapi.yaml#/components/schemas/Context'
  meta:
    allOf:
      - $ref: '../common/metaList.yaml'
    description: Метаданные о выдаче
  rows:
    type: array
    description: Массив позиций Счета поставщику
    items:
      $ref: '../../../openapi.yaml#/components/schemas/InvoiceInPosition'
