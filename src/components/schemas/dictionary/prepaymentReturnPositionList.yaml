type: object
description: Список позиций Возврата предоплаты
x-implements: IEntityList
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../../../openapi.yaml#/components/schemas/Context'
  meta:
    allOf:
      - $ref: '../common/metaList.yaml'
    description: Метаданные о выдаче
  rows:
    type: array
    description: Массив позиций Возврата предоплаты
    nullable: true
    items:
      $ref: '../../../openapi.yaml#/components/schemas/PrepaymentReturnPosition'
