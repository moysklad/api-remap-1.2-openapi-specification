type: object
description: |
  Смена (retailshift). Заглушка: в спецификации нет полного описания сущности;
  при развёртывании в ответе API возможны дополнительные поля.
x-entity-static-builder:
  methodParams:
    - "id"
  href:
    - path: "entity"
    - path: "retailshift"
    - param: "id"
  type: "retailshift"
properties:
  meta:
    description: Метаданные смены
    $ref: '../common/meta.yaml'
  id:
    type: string
    format: uuid
    description: ID смены
    readOnly: true
  accountId:
    type: string
    format: uuid
    description: ID учетной записи
    readOnly: true
  name:
    type: string
    maxLength: 255
    description: Наименование смены
  moment:
    type: string
    description: Дата смены
  retailStore:
    description: Метаданные точки продаж
    nullable: true
    type: object
    allOf:
      - $ref: '../../../openapi.yaml#/components/schemas/RetailStore'
  payments:
    type: array
    description: Связанные платежи
    nullable: true
    items:
      anyOf:
        - $ref: '../../../openapi.yaml#/components/schemas/PaymentIn'
        - $ref: '../../../openapi.yaml#/components/schemas/CashIn'
