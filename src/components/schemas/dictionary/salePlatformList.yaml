type: object
description: Список площадок для продаж
x-implements: IEntityList
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../common/context.yaml'
  meta:
    description: Метаданные о выдаче
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    description: Массив площадок для продаж
    items:
      $ref: '../../../openapi.yaml#/components/schemas/SalePlatform'
