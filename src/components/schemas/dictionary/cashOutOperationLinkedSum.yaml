type: object
description: |
  Поле суммы по связанной операции расходного ордера. Комбинируется через `allOf` с телом документа
  в элементах `operations[]`.
required:
  - linkedSum
properties:
  linkedSum:
    type: number
    format: double
    description: Сумма, оплаченная по данному документу
