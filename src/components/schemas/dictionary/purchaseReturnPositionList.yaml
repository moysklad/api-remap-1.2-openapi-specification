type: object
description: Список позиций Возврата поставщику
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../../../openapi.yaml#/components/schemas/Context'
  meta:
    allOf:
      - $ref: '../common/metaList.yaml'
    description: Метаданные о выдаче
  rows:
    type: array
    description: Массив позиций Возврата поставщику
    items:
      $ref: '../../../openapi.yaml#/components/schemas/PurchaseReturnPosition'
