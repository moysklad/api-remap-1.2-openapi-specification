type: object
description: Список позиций Прайс-листа
x-implements: EntityList
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../../../openapi.yaml#/components/schemas/Context'
  meta:
    description: Метаданные о выдаче
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    description: Массив позиций Прайс-листа
    nullable: true
    items:
      $ref: '../../../openapi.yaml#/components/schemas/PriceListPosition'
