type: object
nullable: true
description: Накладные расходы на уровне документа
properties:
  sum:
    type: number
    format: double
    description: Сумма в копейках
  distribution:
    $ref: '../../../openapi.yaml#/components/schemas/OverheadDistribution'
