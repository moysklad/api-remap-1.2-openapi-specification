type: object
allOf:
  - $ref: '../../../openapi.yaml#/components/schemas/AttributeAbstract'
properties:
  type:
    type: string
    enum: [customentity, contract, counterparty, project, store, employee, product]
  value:
    properties:
      meta:
        $ref: '../common/meta.yaml'
      name:
        type: string
        description: Наименование элемента справочника