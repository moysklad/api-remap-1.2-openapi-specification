type: object
description: Изображение
properties:
  meta:
    $ref: '../common/meta.yaml'
  title:
    type: string
    maxLength: 255
    description: Название изображения
  filename:
    type: string
    maxLength: 255
    description: Имя файла
  content:
    type: string
    description: Изображение, закодированное в Base64
  size:
    type: integer
    format: int64
    description: Размер файла в байтах
    readOnly: true
  updated:
    type: string
    #    format: date-time  # API использует формат: YYYY-MM-DD HH:MM:SS.mmm
    description: Время последнего изменения
    readOnly: true
  tiny:
    description: Метаданные уменьшенного изображения
    $ref: '../common/meta.yaml'
  miniature:
    description: Миниатюра изображения
    $ref: '../common/meta.yaml'