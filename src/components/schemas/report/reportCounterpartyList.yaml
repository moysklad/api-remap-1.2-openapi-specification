type: object
description: Отчет «Показатели контрагентов»
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../common/context.yaml'
  meta:
    description: Метаданные о выдаче
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    description: Массив JSON объектов, представляющих отчеты по отдельным контрагентам
    items:
      $ref: '../../../openapi.yaml#/components/schemas/ReportCounterparty'
