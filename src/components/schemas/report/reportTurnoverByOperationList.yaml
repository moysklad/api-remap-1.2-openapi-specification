type: object
description: Отчет «Обороты по товару с детализацией по документам»
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../common/context.yaml'
  meta:
    description: Метаданные о выдаче
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    description: Массив строк отчета оборотов с детализацией по документам
    items:
      $ref: '../../../openapi.yaml#/components/schemas/ReportTurnoverByOperation'
