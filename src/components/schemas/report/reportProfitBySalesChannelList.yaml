type: object
description: Отчет «Прибыльность по каналам продаж»
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../common/context.yaml'
  meta:
    description: Метаданные о выдаче
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    description: Массив строк отчета прибыльности по каналам продаж
    items:
      $ref: '../../../openapi.yaml#/components/schemas/ReportProfitBySalesChannel'
