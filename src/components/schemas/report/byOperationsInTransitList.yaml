type: object
description: Отчет по документам номенклатуры, отображающий ожидания
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../common/context.yaml'
  meta:
    description: Метаданные о выдаче
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    description: Массив строк отчета с ожиданиями
    items:
      $ref: '../../../openapi.yaml#/components/schemas/ByOperationsInTransit'
