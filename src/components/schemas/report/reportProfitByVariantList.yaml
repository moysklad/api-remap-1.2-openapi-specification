type: object
description: Отчет «Прибыльность по модификациям»
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../common/context.yaml'
  meta:
    description: Метаданные о выдаче
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    description: Массив строк отчета прибыльности по модификациям
    items:
      $ref: '../../../openapi.yaml#/components/schemas/ReportProfitByVariant'
