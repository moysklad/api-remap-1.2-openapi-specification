type: object
description: Отчет «Остатки по документам»
properties:
  context:
    description: Метаданные о сотруднике, выполнившем запрос
    $ref: '../common/context.yaml'
  meta:
    description: Метаданные о выдаче
    $ref: '../common/metaList.yaml'
  rows:
    type: array
    description: Массив строк отчета с остатками по документам
    items:
      $ref: '../../../openapi.yaml#/components/schemas/StockByOperation'
