get:
  operationId: getRoleWorker
  tags:
    - Employees
  summary: Получить роль сотрудника производства
  description: Запрос на получение роли сотрудника производства. Роль доступна только при подключенной опции Управление производством.
  parameters:
    - $ref: '../../../components/headers.yaml#/AcceptHeader'
    - $ref: '../../../components/headers.yaml#/AcceptEncoding'
  responses:
    '200':
      description: Успешный запрос
      content:
        application/json:
          schema:
            $ref: '../../../openapi.yaml#/components/schemas/EmployeeRole'
    default:
      $ref: '../../../components/responses.yaml#/CommonError'
    
    
