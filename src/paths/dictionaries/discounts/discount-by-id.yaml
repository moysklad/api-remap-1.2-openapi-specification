put:
  operationId: updateDiscountRoundingById
  tags:
    - Discounts
  summary: Изменить округление копеек
  description: Запрос на изменение округления копеек
  parameters:
    - $ref: '../../../components/parameters.yaml#/entityId'
    - $ref: '../../../components/headers.yaml#/AcceptHeader'
    - $ref: '../../../components/headers.yaml#/AcceptEncoding'
    - $ref: '../../../components/headers.yaml#/ContentTypeJson'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../../components/schemas/dictionary/discount.yaml'
  responses:
    '200':
      description: Успешный запрос
      content:
        application/json:
          schema:
            $ref: '../../../components/schemas/dictionary/discount.yaml'
    default:
      $ref: '../../../components/responses.yaml#/CommonError'

