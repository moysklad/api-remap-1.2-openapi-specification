get:
  operationId: getMetadata
  tags:
    - Products
  summary: Получить метаданные товаров
  description: Запрос на получение метаданных товаров
  parameters:
    - $ref: '../../../components/headers.yaml#/AcceptHeader'
    - $ref: '../../../components/headers.yaml#/AcceptEncoding'
  responses:
    '200':
      description: Успешный запрос
      content:
        application/json:
          schema:
            $ref: '../../../components/schemas/common/metadata.yaml'
    default:
      $ref: '../../../components/responses.yaml#/CommonError'