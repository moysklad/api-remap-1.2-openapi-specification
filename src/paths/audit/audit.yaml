get:
  operationId: getAuditContexts
  tags:
    - Audit
  summary: Получить контексты аудита
  description: |
    Возвращает общую ленту изменений (контексты аудита).
    Поддерживает пагинацию и фильтрацию через параметр `filter`.
  parameters:
    - name: limit
      in: query
      description: Максимальное количество контекстов в выдаче (допустимые значения 1-100)
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    - name: offset
      in: query
      description: Отступ в выдаваемом списке контекстов
      schema:
        type: integer
        minimum: 0
        default: 0
    - $ref: '../../components/parameters.yaml#/filter'
    - $ref: '../../components/headers.yaml#/AcceptHeader'
    - $ref: '../../components/headers.yaml#/AcceptEncoding'
  responses:
    '200':
      description: Успешный запрос. JSON представление списка контекстов аудита
      content:
        application/json:
          schema:
            $ref: '../../openapi.yaml#/components/schemas/AuditList'
    default:
      $ref: '../../components/responses.yaml#/CommonError'
