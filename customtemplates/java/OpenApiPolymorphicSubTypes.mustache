{{>licenseInfo}}
package {{polymorphicPackage}};

import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;

/**
 * Maps discriminator values to concrete subtype classes for {@link OpenApiPolymorphicTypeInfo}.
 * Analogous to {@link com.fasterxml.jackson.annotation.JsonSubTypes}.
 */
@Target(ElementType.TYPE)
@Retention(RetentionPolicy.RUNTIME)
{{>generatedAnnotation}}
public @interface OpenApiPolymorphicSubTypes {
  Type[] value();

  @Target(ElementType.TYPE)
  @Retention(RetentionPolicy.RUNTIME)
  @interface Type {
    Class<?> value();

    String name();
  }
}
