# Проверка shaded/slim Java SDK артефактов
sdk-verify-java-shaded-slim:
  extends: .maven-jdk17-image
  stage: test
  needs:
    - job: generate-sdk-java
      artifacts: true
  rules:
    - if: '$SDK_LANGUAGES == "" || $SDK_LANGUAGES =~ /java/'
      when: on_success
  script:
    - |
      if [ -d "clients/java" ]; then
        echo "=== Verify Java SDK shaded/slim artifacts ==="
        sh scripts/sdk-verify-java-shaded-slim.sh
      else
        echo "WARNING: clients/java directory not found"
        exit 1
      fi
