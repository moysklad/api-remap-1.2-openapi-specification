# MCP Сервер для МойСклад API

MCP (Model Context Protocol) сервер для работы с OpenAPI спецификацией МойСклад API. Предоставляет AI-ассистентам структурированный доступ к информации о методах API, эндпоинтах, схемах данных и кодах ошибок.

## Возможности

- 🔍 **Поиск эндпоинтов** - поиск по путям, названиям, описаниям и тегам
- 📋 **Информация об эндпоинтах** - полная информация о методах, параметрах, схемах запроса/ответа
- 📊 **Схемы данных** - детальная информация о полях, типах, обязательности
- 🏷️ **Группировка по тегам** - список всех эндпоинтов, сгруппированных по тегам
- ❌ **Коды ошибок** - информация о возможных ошибках и их структуре
- 📝 **Примеры** - примеры значений полей из спецификации

## Быстрый старт

```bash
# 1. Установите зависимости
cd mcp-server
pip install -r requirements.txt

# 2. Проверьте, что спецификация доступна
ls ../src/openapi.yaml

# 3. Сервер готов к использованию!
# Добавьте конфигурацию в настройки вашего MCP-клиента (см. ниже)
```

## Установка

### Требования

- Python 3.8+
- pip

### Шаги установки

1. Установите зависимости:

```bash
cd mcp-server
pip install -r requirements.txt
```

2. Убедитесь, что файл спецификации находится по пути `../src/openapi.yaml` относительно `mcp-server/`

## Использование

### Локальный запуск (stdio)

MCP сервер работает через stdio транспорт. Для подключения к нему из IDE (например, Cursor) нужно добавить конфигурацию в настройки MCP.

#### Пример конфигурации для Cursor

Добавьте в файл настроек MCP (обычно `~/.cursor/mcp.json` или в настройках IDE):

```json
{
  "mcpServers": {
    "moysklad-api": {
      "command": "python",
      "args": [
        "/путь/к/проекту/api-remap-1.2-openapi-specification/mcp-server/server.py"
      ]
    }
  }
}
```

Или если используете виртуальное окружение:

```json
{
  "mcpServers": {
    "moysklad-api": {
      "command": "/путь/к/venv/bin/python",
      "args": [
        "/путь/к/проекту/api-remap-1.2-openapi-specification/mcp-server/server.py"
      ]
    }
  }
}
```

### Тестирование вручную

Для тестирования сервера можно использовать простой скрипт:

```python
import asyncio
from mcp.server.stdio import stdio_server
from server import server

async def main():
    async with stdio_server() as (read_stream, write_stream):
        await server.run(
            read_stream,
            write_stream,
            server.create_initialization_options()
        )

if __name__ == "__main__":
    asyncio.run(main())
```

## Доступные инструменты

### 1. `get_endpoint_info`

Получить полную информацию об эндпоинте API.

**Параметры:**
- `path` (string, обязательный) - путь эндпоинта, например `/entity/product`
- `method` (string, обязательный) - HTTP метод: GET, POST, PUT, DELETE, PATCH

**Пример использования:**
```
get_endpoint_info({
  "path": "/entity/product",
  "method": "POST"
})
```

**Возвращает:**
- Описание метода
- Параметры запроса (query, path, headers)
- Схему тела запроса (если есть)
- Все возможные ответы (успешные и ошибки)
- Структуру ошибок

### 2. `search_endpoints`

Найти эндпоинты по ключевым словам. Ищет в путях, названиях, описаниях и тегах методов.

**Параметры:**
- `query` (string, обязательный) - поисковый запрос

**Пример использования:**
```
search_endpoints({
  "query": "product"
})
```

**Возвращает:**
- Список найденных эндпоинтов
- Информацию о том, по каким критериям найден каждый эндпоинт

### 3. `get_schema_fields`

Получить список полей схемы с типами, обязательностью, примерами и описаниями.

**Параметры:**
- `schema_name` (string, обязательный) - имя схемы, например `Product`, `Group`, `Counterparty`

**Пример использования:**
```
get_schema_fields({
  "schema_name": "Product"
})
```

**Возвращает:**
- Описание схемы
- Список обязательных полей
- Список необязательных полей
- Типы данных
- Примеры значений
- Enum значения (если есть)

### 4. `list_endpoints`

Получить список всех эндпоинтов API, сгруппированных по тегам.

**Параметры:**
- `tag` (string, опциональный) - фильтр по тегу. Если не указан, вернет все эндпоинты

**Пример использования:**
```
list_endpoints({
  "tag": "Products"
})
```

**Возвращает:**
- Список эндпоинтов, сгруппированных по тегам
- Или детальную информацию об эндпоинтах конкретного тега

## Структура проекта

```
mcp-server/
├── server.py              # Основной MCP сервер
├── openapi_parser.py      # Парсер OpenAPI спецификации с резолвером $ref
├── requirements.txt       # Зависимости Python
└── README.md             # Документация
```

## Как это работает

1. **Парсинг спецификации**: При запуске сервер загружает OpenAPI спецификацию из `src/openapi.yaml`
2. **Разрешение ссылок**: Все `$ref` ссылки автоматически разрешаются, включая ссылки на другие файлы
3. **Индексация**: Эндпоинты и схемы индексируются для быстрого поиска
4. **Обработка запросов**: MCP инструменты обрабатывают запросы от AI-ассистента и возвращают структурированную информацию

## Особенности реализации

- **Автоматический парсинг**: Полностью автоматический парсинг OpenAPI спецификации без ручной настройки
- **Резолвер $ref**: Поддержка всех типов ссылок в OpenAPI (относительные пути, внутренние ссылки, комбинированные)
- **Детальная информация**: Возвращает полную информацию о методах, включая примеры, типы, обязательность полей
- **Обработка ошибок**: Детальная информация о кодах ошибок и их структуре

## Разработка

### Добавление новых инструментов

Для добавления нового инструмента:

1. Добавьте обработчик в `server.py`:

```python
async def handle_new_tool(arguments: Dict[str, Any]) -> List[TextContent]:
    # Ваша логика
    return [TextContent(type="text", text=result)]
```

2. Зарегистрируйте инструмент в `list_tools()`:

```python
Tool(
    name="new_tool",
    description="Описание инструмента",
    inputSchema={...}
)
```

3. Добавьте обработчик в `call_tool()`:

```python
handlers = {
    ...
    "new_tool": handle_new_tool,
}
```

## Лицензия

Проект следует лицензии основного репозитория.
