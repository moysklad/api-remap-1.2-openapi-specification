{
  "name": "moysklad-sdk-tests-javascript",
  "version": "1.0.0",
  "description": "JavaScript SDK tests for MoySklad API",
  "scripts": {
    "test": "jest",
    "test:golden": "jest --testPathPattern=golden",
    "test:smoke": "jest --testPathPattern=smoke"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "axios": "^1.6.0"
  }
}
